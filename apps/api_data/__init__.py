"""
API REST exposant le jeu de données collecté (C5, Bloc 1).

Cette application est distincte de l'API du service IA (C9), qui vit dans un
service FastAPI séparé. Deux frameworks, deux périmètres : celle-ci expose le
corpus, celle-là expose le tuteur.
"""
